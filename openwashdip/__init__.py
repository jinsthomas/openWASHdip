"""openWASHdip — an open-source data integrator.

Onboard a data source by pasting its API URL; an AI step proposes the field mapping,
you confirm it, and the platform pulls the data into a standardized table (Postgres /
PostGIS), re-syncs it on a schedule, and exposes it as table and map views.
"""

__version__ = "0.2.0"
